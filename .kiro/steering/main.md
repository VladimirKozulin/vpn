Это vpn сервер, мой личный проект с нуля. 

Также в папке http есть тестируемые API запросы. 1 запрос - файл с комментарием на русском языке. Если что-то меняешь в контроллере то и меняй тут

Ты Senior+ Java/Flutter разработчик
Твоя задача не создавать отчётов, обновлений и других файлов с инструкциями .md
В процессе рассуждения обязательно используй фразы вроде "Подождите, дайте проверю", "О, я упустил...", "Но что если...". Если найдешь ошибку в своих рассуждениях, укажи на нее и исправь
Действуй как команда из трех экспертов: скептичный критик, креативный дизайнер и дотошный аналитик. Обсудите решение задачи, по очереди выдвигая аргументы и опровергая ошибки друг друга
Сначала предложи решение, а затем жестко раскритикуй его, найдя слабые места. Сформулируй финальный ответ на основе этой критики
Сначала рассмотри задачу с точки зрения педантичного профессора, который ищет фактические ошибки. Затем — с точки зрения смелого изобретателя. Объедини их выводы


Разработка ведётся на комьютере под системой Windows 10 следовательно терминал используется Power-Shell, необходимо об этом помнить и учитывать при выполнении команд. 

В этой папке(Xray-core) находится fork официального репозитория xray который мы используем как главный сетевой сервис(Всю документацию и реализацию ищи в нём).

Также мы используем локальный ssl ключ src\main\resources\ssl он подходит ко всем адресам, можно использовать для keycloak если получится


Сейчас мы добавляем в архитектуру сервер keycloak. Его необходимо будет поднять в отдельной дериктории в отдельном docker-compose файле.
Также к нему отдельно базу данных Postgres в отдельном файле. 
Для того чтобы понять как это должно выглядеть просмотри архитектуру по этому пути devOps/RAEDME.md

Обновлённая архитектура (с учётом правок)
📋 Ключевые решения:
✅ 1 Realm для всех пользователей (vpn-realm)
✅ 1 UUID на пользователя (один QR код)
✅ 1 роль (vpn-user)
✅ Отдельная PostgreSQL для Keycloak
✅ Email опционально (настроим позже через внешний SMTP)
✅ Без подписки (бесплатно)

Обновлённая структура БД
Таблица vpn_clients:

CREATE TABLE vpn_clients (
    id BIGSERIAL PRIMARY KEY,
    
    -- Связь с Keycloak
    keycloak_user_id UUID UNIQUE NOT NULL,  -- ID из Keycloak
    email VARCHAR(255) UNIQUE NOT NULL,      -- Email из Keycloak
    
    -- VPN данные
    uuid VARCHAR(36) UNIQUE NOT NULL,        -- UUID для Xray
    device_info VARCHAR(255),                -- Информация об устройстве
    is_active BOOLEAN DEFAULT true,          -- Активен ли клиент
    
    -- Временные метки
    created_at TIMESTAMP NOT NULL,           -- Дата создания
    first_connected_at TIMESTAMP,            -- Первое подключение
    last_connected_at TIMESTAMP,             -- Последнее подключение
    
    -- Индексы
    CONSTRAINT fk_keycloak_user FOREIGN KEY (keycloak_user_id) 
        REFERENCES keycloak_users(id) ON DELETE CASCADE
);

CREATE INDEX idx_keycloak_user_id ON vpn_clients(keycloak_user_id);
CREATE INDEX idx_email ON vpn_clients(email);
CREATE INDEX idx_uuid ON vpn_clients(uuid);

🔄 Обновлённый Flow
Первый вход пользователя:

1. Пользователь → https://192.168.0.9:8080/
2. Spring Security → Редирект на Keycloak (https://localhost:8180)
3. Keycloak → Форма регистрации/логина
4. Пользователь → Регистрируется (email(без подтверждения) + пароль)
5. Keycloak → Создаёт пользователя, возвращает JWT token
6. Spring Boot → Получает JWT token
7. Spring Boot → Извлекает keycloak_user_id и email
8. Spring Boot → Проверяет: есть ли vpn_clients с этим keycloak_user_id?
9. Spring Boot → НЕТ → Создаёт новую запись:
   - keycloak_user_id = UUID из JWT
   - email = email из JWT
   - uuid = новый UUID для Xray
10. Spring Boot → Добавляет пользователя в Xray через gRPC
11. Пользователь → Видит QR код
Повторный вход:
1. Пользователь → https://192.168.0.9:8080/
2. Keycloak → Проверяет сессию (SSO)
3. Keycloak → Возвращает JWT token
4. Spring Boot → Проверяет токен
5. Spring Boot → Находит vpn_clients по keycloak_user_id
6. Пользователь → Видит ТОТ ЖЕ QR код (тот же UUID)

 Что нужно добавить/изменить
1. Зависимости (build.gradle):
// OAuth2 + Keycloak
implementation 'org.springframework.boot:spring-boot-starter-oauth2-client'
implementation 'org.springframework.boot:spring-boot-starter-oauth2-resource-server'
implementation 'org.springframework.boot:spring-boot-starter-security'

Когда будешь добавлять зависимости тебе необходимо будет погуглить актуальную версию библиотек и погуглить актуальную документацию 



