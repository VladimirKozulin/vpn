

services:
  vpn-server:
    build:
      context: .
      dockerfile: Dockerfile.dev
      cache_from:
        - vpn-server:latest
    container_name: vpn-server
    ports:
      - "8080:8080"
      - "443:443"
    env_file:
      - .env
    volumes:
      - .:/app
      - gradle-cache:/home/gradle/.gradle
    networks:
      - vpn-network
    dns:
      - 8.8.8.8
      - 8.8.4.4

volumes:
  gradle-cache:

networks:
  vpn-network:
    driver: bridge
